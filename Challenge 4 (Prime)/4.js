function indexPrime(param) {
    function checkPrime(nn) {
        if (nn < 2) {
            return false;
        }

        for (let ii = 2; ii < nn; ii++) {
            const element = array[ii];
            
        }
    }
    
}

console.log((indexPrime(500)));
