function sentencesManipulation(sentence) {
    // var res = sentence;

    var pergi = sentence.substr(4, 5);
    var pergix = pergi.charArt(0);
    var res = console.log(pergix);

    return res;
    
}

sentencesManipulation('ibu pergi ke pasar bersama aku');